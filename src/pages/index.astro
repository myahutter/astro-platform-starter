<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Redirecting...</title>
  <!-- Google tag (gtag.js) -->
  <script async src="https://www.googletagmanager.com/gtag/js?id=G-CSDMWS894L"></script>
  <script>
    window.dataLayer = window.dataLayer || [];
    function gtag(){dataLayer.push(arguments);}
    gtag('js', new Date());

    const params = new URLSearchParams(window.location.search);
    const source = params.get("utm_source") || "QR";
    const medium = params.get("utm_medium") || "qr";
    const campaign = params.get("utm_campaign") || "SOF Week 2025";
    const content = params.get("utm_content") || "unknown";

    gtag('config', 'G-CSDMWS894L', {
      'campaign_source': source,
      'campaign_medium': medium,
      'campaign_name': campaign,
      'campaign_content': content
    });

    // Get URL parameter to determine where to redirect
    const target = params.get("target");
    let redirectUrl = "";

    if (target === "l3harris") {
      redirectUrl = "https://www.l3harris.com/all-capabilities/tactical-network-rover-tnr-2i-wideband-isr-handheld-transceiver";
    } else if (target === "bdatech") {
      redirectUrl = "https://www.bdatech.com/4-port-hub-ii";
    } else if (target === "mohoc") {
      redirectUrl = "https://www.mohoc.com/mohoc-2/";
    } else if (target === "raintech") {
      redirectUrl = "https://raintechsolutions.com/product-solutions/hail/";
    } else if (target === "viasat") {
      redirectUrl = "https://www.viasat.com/products/cybersecurity/mobile-enterprise-security/";
    } else if (target === "kagwerks") {
      redirectUrl = "https://kagwerks.com/dock/";
    } else if (target === "omtac") {
      redirectUrl = "https://omtac-defense.com/";
    } else if (target === "xcover6") {
      redirectUrl = "https://www.samsung.com/us/business/mobile/phones/galaxy-xcover-pro/";
    } else if (target === "s23te") {
      redirectUrl = "https://www.samsung.com/us/business/solutions/industries/government/tactical-edition/";
    } else if (target === "tabactive5") {
      redirectUrl = "https://www.samsung.com/us/business/solutions/industries/government/tactical-edition/";
    } else if (target === "watch7") {
      redirectUrl = "https://www.samsung.com/us/watches/galaxy-watch7/";
    } else if (target === "glenair") {
      redirectUrl = "https://www.glenair.com/star-pan/index.htm";
    } else if (target === "gox") {
      redirectUrl = "https://www.goxlabs.com/government";
    } else if (target === "galvion") {
      redirectUrl = "https://www.galvion.com/";
    } else if (target === "juggernaut") {
      redirectUrl = "https://juggernautcase.com/categories/velox-ecosystem.html";
    } else {
      redirectUrl = "https://willowy-gumdrop-f8cf71.netlify.app"; // fallback
    }

    setTimeout(function () {
      window.location.href = redirectUrl;
    }, 1500);
  </script>
</head>
<body>
  <p>Redirecting...</p>
</body>
</html>
